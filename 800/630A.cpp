#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; cin >> n;
    // this was not included in the constraint but it can be asked
    if(n == 1)
    cout << 5;
    else 
    cout << 25;

    return 0;
}